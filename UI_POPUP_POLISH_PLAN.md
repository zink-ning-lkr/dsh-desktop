# DSH Desktop 弹窗视觉打磨方案(第四轮)

> 基线:v0.7.0(2026-09-06 全量走查)· 范围:全部 9 个弹出/浮层表面的**纯视觉层**——文字排版(字体/行距/对齐/可读性)、按钮位置布局与间距、整体视觉效果(配色/层级/视觉焦点);**不改变任何功能与交互逻辑**。
> 前序:三轮方案(`UI_OPTIMIZATION_PLAN.md` / `UI_OPTIMIZATION_PLAN_V2.md` / `UI_POPUP_OPTIMIZATION_PLAN.md`)已落地焦点管理、按钮位次与危险语义、关闭语义梯度、a11y 语义、i18n 收编、Win11 系统材质;本轮只针对「看得见」的排版与观感,不重复已达标项。
> 参照体系:GitHub Primer(字号地板与色彩层级)、Microsoft Fluent/WinUI(对话框间距节奏、数字对齐)、WCAG 2.1 AA(正文对比度 ≥4.5:1)。

---

## 1. 总体评价

三轮之后弹窗的「骨架」已经统一:按钮规格(min-width 88)、危险色语义、焦点策略、材质透出各窗一致。本轮逐窗口走查排版层后,剩余问题集中在四类:

1. **列对齐残留两处**——accel 与 status 的头部(icon/标题)与内容列横向错位 2–4px,窗内左侧两条竖线不齐;welcome 头部标题 13px 离群于公共 14px 规格。
2. **数字与换行策略缺失**——进度百分比每 150ms 刷新、分段数拖动实时变化,但数字用默认比例字宽逐帧微抖;5 处 prose 文本用 `word-break: break-all`,英文单词被任意腰斩。
3. **微字号与对比度尾巴**——accel 分区标签 10px 是全应用最小字号;menu 只读行(0.55)与 loading 日志尾(0.8)两处叠加透明度后低于 AA 下限。
4. **规格离群值**——决策按钮 88px 规则未覆盖 report 动作组与 loading 慢启动行;report 两块面板 8px 圆角在 6/7/10 级距外;loading 按钮间距 10 与全局 8 不一。

全部为低风险 CSS 级修改;唯一 P0 是 accel 校验错误行可能被挤压截断的布局缺陷。

## 2. 走查结论(逐窗口)

| 窗口 | 排版 | 按钮 | 视觉/层级 | 本轮动作 |
|---|---|---|---|---|
| 对话框 dialog | 标题/正文/详情三级字号 15/13/12、行距 1.3/1.5/1.5 齐备 | ✅ 位次/等宽 88px 达标 | ✅ 图标+彩底圆标层级清晰 | 仅 X4-2 换行策略 |
| 任务中心 status | 详情/行标题行距齐备 | ✅ 结果按钮 btn-row 达标 | ⚠️ 头部列错位 2px;meta 数字抖动 | X4-1 / X4-4 / ST4-1 |
| 菜单弹层 menu | 行高 30px、快捷键右列对齐达标 | — | ⚠️ 只读行对比度不足 | X4-6①(行高/分隔线被 UITEST 锁定,不动) |
| 首启欢迎 welcome | hero 层级清晰 | ✅ 主按钮全宽+退出居右达标 | ⚠️ 标题 13px 离群;hint 折行左对齐 | WEL4-1 / WEL4-2 |
| 错误报告 report | cause/advice 行距齐备 | ⚠️ 动作组未挂 88px 规则 | ⚠️ 面板圆角 8 离群;日志 11.5px 偏小 | X4-3 / X4-7 / RPT4-1 |
| 加速设置 accel | hint/note 行距齐备 | ✅ 主左次右达标 | ⚠️ 头部列错位 4px;tag 10px;错误行可截断 | ACC4-1 / X4-4 / X4-5 / X4-1 |
| 启动加载 loading | 阶段/提示层级清晰 | ⚠️ 间距 10 离群、未挂 88px | ⚠️ 日志尾对比度不足 | LDG-4-1 / X4-3 / X4-6② |
| 下拉把手 reveal | — | — | ✅ | REV4-1(文案收编) |
| 标题栏 titlebar | 12px 栏高规格合理 | ✅ | ✅(.72 透明度梯度为本轮参照) | 已达标,不动 |

## 3. 优化项

> 每项含:问题定位 → 优化目标 → 改进措施(对应代码)→ 预期效果。P0=布局缺陷;P1=可读性/一致性;P2=打磨。

### ACC4-1【布局·校验错误行可能被挤压截断】P0

- **问题定位**:accel.html:47 `.err`(镜像校验错误)位于按钮行 `.row`(accel.html:87-91)内,无 flex/min-width 约束——flex 子项默认 `min-width:auto`,长错误文案按最长单词收缩,与「保存/清除」按钮挤压或溢出容器(accel.html:17 `.content` 仅纵向滚动,横向内容被裁)。
- **目标**:任意长度的校验错误完整可见,按钮不被挤压。
- **措施**:`.err` 增加 `flex: 1; min-width: 0;`——有错误时占按钮行剩余宽度并正常折行;空值时零影响。
- **预期效果**:主进程校验器的长错误文案不再截断;与 ACC-2(第三轮「错误就地呈现」)的语义闭环在视觉上成立。

### X4-1【排版·数字文本启用等宽数字】P1

- **问题定位**:status.html:48 `.meta` 承载进度百分比/体积,进度帧每 150ms 刷新(status.html:223-224);accel.html:32-35 `.seg-n` 承载分段数,拖动滑杆实时变化——Segoe UI 默认比例数字宽度随数字组合变化,逐帧/逐字微抖。
- **目标**:数值刷新与拖动时文本零横跳。
- **措施**:status.html:48 与 accel.html:33 各加 `font-variant-numeric: tabular-nums;`。
- **预期效果**:百分比/分段数变化时字宽恒定;高频刷新区视觉稳定。

### X4-2【排版·prose 换行从 break-all 改为词边界优先】P1

- **问题定位**:dialog.html:22 `.detail`、status.html:26 `.detail` / :55 `.rdetail` / :72 `.ldetail`、report.html:27-28 `.cause` 共 5 处用 `word-break: break-all`——中英混排时 "error"/"ECONNRESET" 等英文单词在任意字符处腰斩,损害可读性;break-all 只对超长 token(URL/路径)才是必要的。
- **目标**:优先按词边界换行,超长 token 仍可断行防溢出;CJK 行为不变。
- **措施**:5 处 `word-break: break-all` 改为 `overflow-wrap: anywhere`。report.html:37 `.log` 保持 break-all(日志为 token 密集文本,整行均匀填充优先)。
- **预期效果**:错误详情/建议/结果详情中的英文不再词中断裂;长 URL/路径仍不撑破容器;各文本块均有 max-height 或自适应高度,无窗口高度回归(status 186/250 锁定档内文本块自带滚动)。

### X4-3【按钮·决策按钮 88px 规则补齐 report/loading】P1

- **问题定位**:ui.css:127 `.btn-row .btn { min-width: 88px }` 已覆盖对话框 foot 与状态窗结果按钮(status.html:209),但 report.html:72 `#actions`(重试/退出)与 loading.html:88 `.slow-btns`(查看日志/重试/取消并退出)未挂 `.btn-row`——同类「决策按钮行」规格不一致,短标签按钮(重试)夹在长标签工具按钮间显小。
- **目标**:跨窗决策按钮同一宽度规格。
- **措施**:report.html:72 `<div id="actions">` 加 `btn-row` 类;loading.html:88 `.slow-btns` 加 `btn-row` 类。列表行内小按钮(`.lbtns`)不参与的既有取舍不变。
- **预期效果**:报告窗「重试」与对话框主按钮观感一致;慢启动救助行动作规格与全局统一;文案增删不再牵动按钮宽度。

### X4-4【对齐·头部列与内容列对齐】P1

- **问题定位**:① accel 头部走公共 `--head-pad` 默认 14px 16px 10px(ui.css:134),而内容列 accel.html:17 `.content` 横向 20px——头部图标 x=16、正文 x=20,窗内左侧错位 4px;② status 头部覆写 `--head-pad: 12px 14px 0`(status.html:16),内容列 status.html:25 `.activity` 横向 16px——错位 2px。dialog(20/20)、report(16/16)、welcome(16/16)均已对齐。
- **目标**:各窗口头部与内容列同一条左基线。
- **措施**:① accel.html 增加 `.win-head { --head-pad: 14px 20px 10px; }`;② status.html:16 改为 `12px 16px 0`。均只动横向值,竖向不变(accel 窗口高 556 固定、status 186/250 锁定档不受影响)。
- **预期效果**:窗内 icon/标题与正文左缘成一条线;三窗头部内边距规格可读地统一。

### X4-5【排版·微字号地板:分区标签 10px → 11px】P1

- **问题定位**:accel.html:21 `.sec-t .tag` `font-size: 10px` 是全应用最小字号(次小为各窗 foot note 的 11px),三个分区标签(桌面端更新/可选/npm)在彩底描边胶囊里偏难读。
- **目标**:全应用小字不低于 11px 地板。
- **措施**:`10px → 11px`,内边距 2px 8px 不变;配套 main.js:963 accelWin 内容高度 556 → 566——实测 .content 可用 470px 在标签增高后余量归零(渲染实测 scrollHeight 470 = clientHeight 470),+10px 恢复呼吸空间(该尺寸非 UITEST 锁定档)。
- **预期效果**:分区标签与报告窗 `.sec-label`(11px)同一地板;日常态内容不再贴边,「下载进行中」提示态的滚动量同步减小。

### RPT4-1【排版·日志预览字号 11.5 → 12px】P1

- **问题定位**:report.html:36 `.log` 11.5px 等宽——报告窗信息量最大的主内容用了偏小字号,主进程日志多含路径/堆栈,长时间阅读吃力(VS Code 终端默认 12px)。
- **目标**:日志预览达到正文级可读性。
- **措施**:`11.5px → 12px`(容器 flex:1 高度不变,仅换行密度变化)。
- **预期效果**:日志字号与正文 detail(12px)一致;行数换行更稀疏一档。

### ST4-1【排版·结果标题补行距】P2

- **问题定位**:status.html:54 `.rtitle` 未设 line-height,继承 body 的 `--font-ui`(/1,status.html:14)——结果标题目前多为单行,i18n 化后出现长标题折行时行距=1 过挤。
- **目标**:折行标题有正常行距。
- **措施**:加 `line-height: 1.35`(与列表行标题 .ltitle 一致,status.html:71)。
- **预期效果**:单行零变化;折行时不拥挤;250px 锁定档内 2 行标题仍安全容纳。

### X4-6【视觉·两处低对比度文本】P2

- **问题定位**:① menu.html:50 `.item.off` opacity .55——托盘菜单只读状态行为 fg1 的 55% 混合,约 4:1,低于 AA;同类「弱化但可读」的既有决策是 titlebar 工作目录的 .72(titlebar.html:51-52 注释,第二轮已把 .5 提到 .72)。② loading.html:65 `.logtail` opacity .8——11px 等宽再打 8 折,fg2 在 bg0 上本已贴近 AA 下限。
- **目标**:次要信息仍明显弱于正文,但达到 AA(≥4.5:1)。
- **措施**:① `.55 → .72`;② 去掉 `opacity: .8`,保留 fg2 原色。
- **预期效果**:只读状态行与日志尾清晰可读;透明度梯度与标题栏(.85/.72)一致。

### X4-7【视觉·面板圆角归级】P2

- **问题定位**:report.html:28 `.cause` 与 report.html:35 `.log` 圆角 8px,不在令牌级距(--radius-s 6 / -m 7 / -l 10)内,是全应用唯一的第三圆角值。
- **目标**:圆角单一来源。
- **措施**:两处 `8px → var(--radius-m)`。
- **预期效果**:面板圆角与其余窗口一致;令牌级距外无游离值。

### WEL4-1【排版·头部标题字号对齐公共规格】P2

- **问题定位**:welcome.html:18 `.head .t` 13px——自绘头部未走公共 `.win-head`,公共标题规格为 14px(ui.css:139),各内容窗标题层级为「对话框 15 / 内容窗 14」。
- **目标**:welcome 标题与公共规格一致。
- **措施**:`13px → 14px`(头部行高由 18px logo 主导,窗口高 560 固定不受影响)。
- **预期效果**:标题尺寸跨窗一致;hero 区 19px 大标题与头部 14px 的层级更分明。

### WEL4-2【排版·提示文本居中】P2

- **问题定位**:welcome.html:25-28 `.act` 为纵向 flex 且 `align-items: center`,但 `.hint` 无 text-align——hint(welcome.html:67,文案较长)折行为多行时左对齐,与 hero 区 `.sub` 的居中(welcome.html:20)不一致。
- **目标**:折行提示与副标题视觉统一。
- **措施**:`.hint` 加 `text-align: center`。
- **预期效果**:多行提示居中呈现;单行零变化。

### LDG-4-1【按钮·慢启动行间距归一】P2

- **问题定位**:loading.html:76 `.slow-btns` gap 10px;全局决策按钮行间距为 8px(dialog.html:24 / report.html:45 / status.html:57)。
- **目标**:间距归一。
- **措施**:`gap: 10px → 8px`(配合 X4-3 挂 `.btn-row`)。
- **预期效果**:救助行动作与全局按钮行节奏一致。

### REV4-1【文案·把手 aria/title 收编 i18n】P2

- **问题定位**:reveal-tab.html:32 `title/aria-label` 硬编码「显示标题栏」——第三轮 X-2 i18n 收编的漏网之鱼;该页经 titlebar-preload.js 加载,`window.__i18n` 可用,且 `menu.showBar` 键已存在(同值,i18n/zh-CN.json)。
- **目标**:文案单一来源全覆盖。
- **措施**:初始属性置空(与 report/accel 的 ✕ 按钮同模式),脚本内经 `__i18n.t('menu.showBar')` 填充 title 与 aria-label。
- **预期效果**:渲染层硬编码文案归零;行为与文案零变化。

## 4. 已达标基线(本轮不动)

- **对话框**:15/13/12 三级字号与 1.3/1.5/1.5 行距、按钮 min-width 88、主操作居左、危险色语义、队列换页动画(dialog.html:83-86)。
- **菜单**:行高 30px / 分隔线 9px 被主进程高度估算锁定(main.js:1332-1336),快捷键右列经 flex 右缘对齐达标;typeahead/APG 语义第三轮已达标。
- **任务中心**:单视图 186/250 高度锁定档(main.js:482-489、739-745),竖向节奏本轮不动;进度条/结果视图配色走公共 `.big.<type>`。
- **标题栏**:12px 栏高规格、工作目录 .72 透明度、跟随 dsh 页面采样色。
- **共享层**:ui.css 令牌(色板/圆角/动效/字栈)、`.btn` 四态、`.win-head`、`.inline-feedback`、焦点环本轮零改动(页面级覆写即达成全部目标)。

## 5. 验证与回归边界

- **几何契约**:status 单视图 186/250、菜单行高 30/9、accel 窗 556 为固定档——本轮全部只动横向值/字重特性/透明度,竖向无变化;dialog 高度经 `dl:rendered` 按真实内容自适应(main.js:901-906),report 列表经 `S.rendered()` 自适应,X4-2 换行策略的变化被天然吸收。
- **UITEST**:d1-choose、tc-fit、accel-btn、menu-open 断言不触及本轮改动面;可按需复跑 `DSH_UITEST` 冒烟确认。
- **主题**:全部修改走既有令牌/透明度,浅色主题(`data-theme="light"`)自动成立,无需另改。
